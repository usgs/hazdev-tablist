'use strict';

// App configuration, used throughout
var config = {
  build: '.build',
  buildPort: 8070,
  dist: 'dist',
  distPort: 8072,
  example: 'example',
  examplePort: 8073,
  livereloadPort: 8079,
  node_modules: 'node_modules',
  src: 'src',
  test: 'test',
  testPort: 8071
};

module.exports = config;