'use strict';

// App configuration, used throughout
var config = {
  build: '.build',
  dist: 'dist',
  example: 'example',
  node_modules: 'node_modules',
  src: 'src',
  test: 'test'
};

module.exports = config;