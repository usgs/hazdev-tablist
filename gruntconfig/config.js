'use strict';

// App configuration, used throughout
var config = {
  build: '.build',
  dist: 'dist',
  node_modules: 'node_modules',
  src: 'src',
  test: 'test',
  tmp: '.tmp'
};

module.exports = config;